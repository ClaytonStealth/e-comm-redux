import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#85129e",
    },
    secondary: {
      main: "##2c9e12",
    },
  },
});

export default theme;
